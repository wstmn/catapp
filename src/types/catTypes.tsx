export interface cat {
    breed: string,
    country: string,
    origin: string,
    coat: string,
    pattern: string,
    favourite: boolean,
    id: string,
    description: string,
    adaptability: string,
    lifeSpan: string
  }
  
  export interface catImage {
    id: string,
    imageURL: string
  }